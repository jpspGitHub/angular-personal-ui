import { LanguageLevel } from '../enums';

export class LanguageItem {
  name: string;
  img: string;
  comprehensionLevel: LanguageLevel;
  expressionLevel: LanguageLevel;
  writtenLevel: LanguageLevel;
}

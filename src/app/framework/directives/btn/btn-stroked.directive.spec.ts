import { BtnStrokedDirective } from './btn-stroked.directive';

describe('BtnStrokedDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnStrokedDirective(undefined, undefined);
    expect(directive).toBeTruthy();
  });
});
